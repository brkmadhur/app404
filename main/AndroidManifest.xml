<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team_404.app_404">

    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SMS_PORTAL"
            android:label="@string/title_activity_sms__portal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SMS_ANALYSER"
            android:label="@string/title_activity_sms__analyser"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SELECT_CONTACTS"
            android:label="@string/title_activity_select__contacts"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>